if flash
  .container
    .row
      if flash.message         
        .col-md-12.alert.alert-success #{flash.message}
      if flash.warning
        .col-md-12.alert.alert-warning #{flash.warning}
      if flash.error
        .col-md-12.alert.alert-danger #{flash.error}